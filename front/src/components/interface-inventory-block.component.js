import React, { useEffect, useRef } from 'react';
import { Game } from '../core/global';

const InterfaceInventoryBlockComponent = () => {
    return (
        <div className="block">
            <p>aze</p>
        </div>
    );
};

export default InterfaceInventoryBlockComponent;
